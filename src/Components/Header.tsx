import './styles/Header.scss';

const Header = () => {
    return (
        <div className='header'>
            <div className={'heading'}>{'Chicago Students Continue to Make Progress'}</div>
        </div>
    );
};

export default Header;